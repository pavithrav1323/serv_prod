<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.serv.serv_app">

    <!-- ========== NETWORK PERMISSIONS ========== -->
    <!-- Required for all network operations -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ========== LOCATION PERMISSIONS ========== -->
    <!-- Required for attendance check-in/out and tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for background location tracking -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ========== FOREGROUND SERVICES ========== -->
    <!-- Required for background location tracking and data sync -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ========== CAMERA PERMISSION ========== -->
    <!-- Required for admin profile picture uploads -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ========== NOTIFICATIONS ========== -->
    <!-- Required for showing notifications to users -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ========== BIOMETRICS ========== -->
    <!-- Required for secure authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <!-- ========== BATTERY OPTIMIZATION ========== -->
    <!-- Required to prevent battery optimization from stopping background services -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Queries for URL launcher and other intents -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <!-- Add any other intents your app needs -->
    </queries>

    <application
        android:name="${applicationName}"
        android:label="SERV"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="s"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true">
        
        <!-- Google Mobile Ads and Services Configuration -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/app_id" />
            
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
            
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />
            
        <meta-data
            android:name="com.google.android.gms.ads.DELAY_APP_MEASUREMENT_INIT"
            android:value="false" />
            
        <!-- Privacy Policy URLs -->
        <meta-data
            android:name="com.google.android.gms.ads.ADMOB_PRIVACY_POLICY_URI"
            android:value="https://www.yourdomain.com/privacy-policy" />
            
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_PRIVACY_POLICY_URI"
            android:value="https://www.yourdomain.com/privacy-policy" />
            
        <meta-data
            android:name="com.google.android.gms.ads.USER_AGENT"
            android:value="SERV App" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google Maps key (if you use it) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA6vu7Fb_znHOCvL3Eshht69Pn-jGEaC90" />

        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false"
            tools:replace="android:exported" />

        <!-- ⚠️ No <service> declarations here; plugin merges them automatically. -->
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>